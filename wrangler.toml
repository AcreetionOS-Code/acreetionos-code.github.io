name = "trumpos-mask"
main = "index.js"
compatibility_date = "2026-04-26"

# Routes managed via Cloudflare dashboard — remove from wrangler to avoid CI conflicts
# routes = []

[vars]
# OPENROUTER_API_KEY is supplied as a secret via wrangler deploy --secret
